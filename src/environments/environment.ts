// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  
};
export const AUTH_URL="http://localhost:8082/api/auth";
export const employeUrl="http://localhost:8082/api/employer";
export const candidatUrl="http://localhost:8082/api/candidats";
export const recruteurUrl="http://localhost:8082/api/recruiter";
export const meetUrl="http://localhost:8082/api/meet";
/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
